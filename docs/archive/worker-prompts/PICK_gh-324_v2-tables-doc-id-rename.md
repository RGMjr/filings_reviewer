You are working gh-324 (primary) and gh-323 (bundled): finishing the doc_id → filing_id rename hygiene started by legacy-038.

## Why bundle these
Both fragments are direct legacy-038 follow-ups in the same naming-confusion class. gh-324 covers the four remaining v2 tables (`v2_segments`, `v2_tables`, `v2_image_assets`, `v2_metric_definitions`) plus `src/extraction_v2/persistence.py` + `src/infra/db.py` callsites. gh-323 covers the `MetricFact` dataclass field at `src/extraction_v2/models.py:320` (currently named `doc_id` but holds Document UUID; dead-but-trap field). The dataclass cleanup naturally rides with the persistence sweep — splitting them creates two PRs that touch the same files.

## Source of truth
- Fragment: `docs/known-issues/gh-324-other-v2-tables-bigint-doc-id-mismatch.md` (read in full before planning)
- Fragment: `docs/known-issues/gh-323-metricfact-doc-id-field-semantic-confusion.md` (read in full before planning)
- Reference: how legacy-038 was done — look at the merged migration `sql/202604282225_rename_v2_metric_facts_doc_id_to_filing_id.sql` and PR #326's diff for the established pattern (information_schema idempotency guard, view-attnum verification, callsite sweep). Also look at PR #331 (gh-328) which fixed the integration-test fallout — apply the same hygiene proactively for these four tables.
- CLAUDE.md (project root) — read fully; obey Implementation Rules and Pre-Implementation Gate. Note `.claude/rules/sql.md` for migration filename convention (timestamp-prefixed, generated by `scripts/new_migration.py`) and the frozen `sql/00-47` integer range.
- Global CLAUDE.md (`~/.claude/CLAUDE.md`) — read; especially "Implementation Rules" and "Planning Rules"
- Project memory at `~/.claude/projects/-Users-rgmarkey-CMASB-Coding-filings-reviewer/memory/MEMORY.md` — read fully and apply

## Workflow
1. **Verify the issues are still relevant.** Re-read both fragments from origin/main. Confirm the four columns still have the misleading name:
   ```bash
   git grep -n 'doc_id' sql/ | grep -E '(v2_segments|v2_tables|v2_image_assets|v2_metric_definitions)'
   ```
   And confirm `MetricFact.doc_id` still exists at `src/extraction_v2/models.py` (line ~320) and is still populated only by `src/extraction_v2/stages/fact_construction.py` and read by no production code.
2. **Plan mode.** The plan must include:
   - **One timestamped migration** renaming `doc_id` → `filing_id` on all four tables, idempotent via `information_schema.columns` guard per-table (mirror the legacy-038 migration). Include any indices that embed the old name.
   - **Callsite sweep:** every reference to the renamed column in `src/extraction_v2/persistence.py`, `src/infra/db.py`, integration tests, scripts, and any `v_analytics_*` view definitions. Per CLAUDE.md, frozen `sql/09`/`sql/38` view files do NOT need recreation (attnum-stable per legacy-038 evidence) — but verify by inspecting the views, don't take it on faith.
   - **gh-323 cleanup:** decide whether to rename `MetricFact.doc_id` (e.g. to `document_uuid`), delete it, or re-purpose it. Recommend rename + remove the misleading `# Filing ID` comment, since the value IS a Document UUID and a downstream caller could legitimately want it. Sweep `fact.doc_id` and `MetricFact(doc_id=...)` constructor sites (per gh-323 fragment, only `fact_construction.py:229` and test fixtures).
   - **Test plan:** integration tests under `tests/integration/extraction_v2/`, `tests/integration/web/`, and `tests/integration/test_migration_safety.py` will exercise the SQL changes. Plan for any test fixture SQL updates.
   - **Documentation:** per global CLAUDE.md "Planning Rules" — explicitly identify docs to update. Likely candidates: `CLAUDE.md` Database section if the column is named there, `.claude/rules/sql.md` if it documents the convention, any architecture docs that name the column. If none apply, state so explicitly.
   Run `/plan-review` before exiting plan mode.
3. **Worktree-first.** First step of implementation: `EnterWorktree fix/gh-324-v2-tables-doc-id-rename`. The PreToolUse hook denies HEAD-moving git ops in the primary tree.
4. **Pre-Implementation Gate** (per global CLAUDE.md). Show the completed checklist and get user approval before writing code.
5. **Tests.** `pytest -x -q --tb=short` must pass — particularly the integration suites that just got fixed for gh-328. Per `feedback_run_recovery_before_verification`, if you see schema-drift errors against the test DB, run dropdb+migrate first rather than debugging code. Per `project_extraction_guard_hook_scope`, the extraction-guard pre-commit hook fires on `src/extraction_v2/persistence.py` — be ready for it to gate.
6. **Update fragment status as part of the same PR.** Both fragments get closed in this PR:
   - gh-324: `status: open` → `resolved`, `autonomy: skip` → `n/a`, `pr_refs: [<this PR #>]`, append `### Resolution`.
   - gh-323: same flips, with `### Resolution` noting the rename was bundled with gh-324.
   Per `project_fragment_only_closure_pattern`. Per `feedback_known_issues_validator_optional_fields`, only `pr_refs` / `gh_issue` / `note` are allowed in frontmatter — do NOT add new fields.
7. **Commit + PR.** Use the project-local `/commit-proj` skill. The PR description should explicitly recital the closures: "Closes #324, closes #323."
8. **Verify auto-merge.** After `/commit-proj` returns, run `gh pr view --json autoMergeRequest`. If unset, run `gh pr merge <PR#> --auto --squash`.

## Out of scope (do NOT expand into)
- Renaming `doc_id` on `v2_metric_facts` — already done in legacy-038 / PR #326.
- Renaming `doc_id` on `v2_documents` — that one IS a UUID and is correctly named.
- Refactoring unrelated `db.py` helpers, persistence logic, or extraction stages.
- Backfilling or rewriting any data — this is a column-rename only; data is unchanged.

## Memory references that apply
- `feedback_verify_issue_status` — verify on origin/main first
- `feedback_run_recovery_before_verification` — schema drift in test DB needs dropdb+migrate, not pytest debugging
- `feedback_hash_rule_change_transition` — N/A here (rename, not hash-rule), but the spirit applies: be sure the migration is byte-safe and idempotent
- `project_extraction_guard_hook_scope` — extraction-guard hook fires on persistence.py changes
- `project_fragment_only_closure_pattern` — fragment frontmatter flip + Resolution section in the same PR
- `feedback_known_issues_validator_optional_fields` — only `pr_refs`, `gh_issue`, `note` allowed when closing
- `feedback_known_issues_pr_refs_int_not_string` — write `- 332`, not `- '#332'`
- `feedback_subagent_midstream_stops` — if you delegate, dispatch a tightly-scoped wrap-up pinned to the worktree if returns are truncated
- `feedback_verify_auto_merge_after_commit` — verify auto-merge is set after /commit-proj

## Return
The PR URL when done.
