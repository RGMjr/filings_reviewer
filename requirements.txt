# SEC Filings Reviewer - Python Dependencies
# ==========================================
#
# Install with: pip install -r requirements.txt
#
# For development: pip install -r requirements.txt && pip install -e .

# -----------------------------------------------------------------------------
# Core Runtime Dependencies
# -----------------------------------------------------------------------------

# PostgreSQL adapter (psycopg3) - Database connectivity for storing filings,
# companies, metrics, and extraction results. Uses binary package for faster install.
psycopg[binary,pool]>=3.1.0

# Environment variable management - Loads DATABASE_URL, API keys from .env files
# Pinned >=1.2.2 for CVE-2026-28684 (fixed in 1.2.2).
python-dotenv>=1.2.2

# Progress bars - Visual feedback for long-running operations
tqdm>=4.66.0

# HTTP client - Used for SEC EDGAR API requests (rate-limited, polite access)
requests>=2.33.0

# S3-compatible object storage client - image cache backing store (Cloudflare R2 in prod,
# local filesystem in dev). Used by src/infra/image_storage.py.
boto3>=1.34.0

# HTML parsing - Extracts text and tables from SEC filing HTML documents
beautifulsoup4>=4.12.0

# Encoding detection - Automatic charset detection for legacy SEC filings
charset-normalizer>=3.3.0

# YAML parsing - Loads metric keyword configuration from config/metric_keywords.yaml
PyYAML>=6.0
types-PyYAML>=6.0

# Round-trip YAML editor — used by scripts/open_pattern_recommendation_pr.py to
# patch config/metric_keywords.yaml in-place while preserving comments + key
# ordering. PyYAML cannot preserve comments; ruamel.yaml is the standard
# round-trip alternative.
ruamel.yaml>=0.18.0

# Faster HTML/XML parser for BeautifulSoup and extraction_v2
# Required by extraction_v2/stages/ingestion.py for 10x faster parsing
lxml>=6.1.0

# PDF parsing - Used by presentation_converter.py for investor presentations
pdfplumber>=0.10.0

# PDF rendering fallbacks (optional, used by presentation_converter.py)
pymupdf>=1.23.0
pypdfium2>=4.0.0

# HuggingFace dataset access (optional, used by huggingface_source.py)
datasets>=2.0.0

# Application monitoring (optional, used by extraction_v2/logging_config.py)
sentry-sdk>=1.0.0

# -----------------------------------------------------------------------------
# Machine Learning Utilities
# -----------------------------------------------------------------------------

# Lightweight TF-IDF + logistic regression metric classifier tooling
joblib>=1.3.0
numpy>=2.4.5
scikit-learn>=1.4.0

# -----------------------------------------------------------------------------
# LLM Integration
# -----------------------------------------------------------------------------

# OpenAI API - For metric extraction and quality scoring
openai>=1.0.0

# Google Gemini vision API - used via VisionProvider abstraction (Wave B2).
# Imported as `from google import genai`. No Vertex/GCP project — uses
# GOOGLE_API_KEY env var for auth.
google-genai>=1.0.0

# Anthropic Claude vision API - used via VisionProvider abstraction (Wave B2).
# Pinned >=0.87.0 for CVE-2026-34450 and CVE-2026-34452 (fixed in 0.87.0).
anthropic>=0.87.0

# Google ID-token validation for the OAuth login flow (PR-A5).
# Provides google.oauth2.id_token.verify_oauth2_token used by
# src/auth/oidc_validate.py to validate signature, aud, and exp.
google-auth>=2.53.0,<3

# Token counting - For cost tracking and context management
tiktoken>=0.5.0

# -----------------------------------------------------------------------------
# Web Application Dependencies
# -----------------------------------------------------------------------------

# Flask - Web framework for human review interface
flask>=3.0.0

# -----------------------------------------------------------------------------
# Production WSGI Server
# -----------------------------------------------------------------------------

# Waitress - Pure Python WSGI server for production deployment
# Cross-platform (Linux, macOS, Windows), thread-based workers
waitress>=3.0.0

# Security headers middleware
flask-talisman>=1.1.0

# HTML sanitization - strips scripts/event handlers from SEC filing HTML before rendering
nh3>=0.2.17

# Image processing - parses pixel dimensions from cached image bytes for feature engineering
Pillow>=12.2.0

# Transitive constraint — pinned to clear CVE-2026-45409 (fixed in 3.15).
idna>=3.15

# Development and test dependencies are in requirements-dev.txt
