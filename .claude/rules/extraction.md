---
paths:
  - "src/extraction/**"
---

# Extraction Module (V1 — Retired)

`src/extraction/` is retired and kept for historical reference only. Do not add new V1 code.

Active pipeline: `src/extraction_v2/` — see `.claude/rules/v2-pipeline.md`.
Keyword config: `config/metric_keywords.yaml` — also covered by v2-pipeline.md.
