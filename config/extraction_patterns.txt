# Canonical list of extraction file patterns.
# Used by commit gates, pre-commit hooks, and agent dispatch rules.
# One pattern per line. Lines starting with # are comments.
#
# A staged file matches if its path contains any of these prefixes.
src/extraction/
src/extraction_v2/
src/review/candidate_generator
src/review/false_positive_filter
src/review/keyword_matching
src/review/pattern_analyzer
config/metric_keywords.yaml
