# src/auth — Authentication and authorization package.
# Stage A (foundation): dev-bypass guard (A6) and CSRF middleware (A4).
# Session management (A3), permission catalog (A2), and enforcement (A4+)
# land in subsequent PRs.
